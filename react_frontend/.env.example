# Backend API Base URL
# This should be set to the Express backend URL
# For development: http://localhost:3001
# For production: This will be set by the deployment agent
REACT_APP_API_BASE_URL=http://localhost:3001

# Site URL for email redirects (used by Supabase if integrated)
# This will be mapped to the final deployment URL by the deployment agent
REACT_APP_SITE_URL=http://localhost:3000
